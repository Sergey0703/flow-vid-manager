services:
  aimediaflow-cat-agent:
    build: .
    container_name: aimediaflow-cat-agent
    restart: unless-stopped
    env_file: .env
    networks:
      - n8n_default

networks:
  n8n_default:
    external: true
