# Supabase Configuration
# Get these values from your Supabase project dashboard
# https://supabase.com/dashboard/project/[YOUR_PROJECT_ID]/settings/api

# Supabase Project URL
VITE_SUPABASE_URL=https://your-project-id.supabase.co

# Supabase Anon/Public Key (safe for client-side usage)
VITE_SUPABASE_PUBLISHABLE_KEY=your_supabase_anon_key_here

# Supabase Project ID (optional, for additional validation)
VITE_SUPABASE_PROJECT_ID=your_project_id_here

# Development/Production Environment
# NODE_ENV=development

# Instructions:
# 1. Copy this file to .env: cp .env.example .env
# 2. Fill in your actual Supabase credentials
# 3. Never commit .env to version control
# 4. Keep .env.example updated when adding new variables

# Brevo (Email) — Contact Form
# Get your API key from app.brevo.com → Settings → API Keys
BREVO_API_KEY=your_brevo_api_key_here
CONTACT_EMAIL=your_email@example.com

# Chatbot — LLM provider
# LLM_PROVIDER: "anthropic" | "openai" | "openrouter"
LLM_PROVIDER=anthropic
# API key for chosen provider
LLM_API_KEY=your_llm_api_key_here
# Model name (e.g. claude-haiku-4-5-20251001 / gpt-4o-mini / openrouter model id)
LLM_MODEL=claude-haiku-4-5-20251001

# Pinecone — Vector DB for RAG
# Get API key from app.pinecone.io → API Keys
PINECONE_API_KEY=your_pinecone_api_key_here
# Index host URL from app.pinecone.io → Indexes → your index → Host
PINECONE_INDEX_HOST=https://aimediaflow-xxxx.svc.aped-xxxx.pinecone.io

# Ingest protection — random secret shared between the admin UI and the API
# Generate with: openssl rand -hex 32
# Set BOTH on Vercel (INGEST_SECRET for the API, VITE_INGEST_SECRET for the frontend build)
INGEST_SECRET=your_random_secret_here
VITE_INGEST_SECRET=your_random_secret_here

# Security Notes:
# - Only use VITE_ prefixed variables (they become public in the browser)
# - Never put service_role keys here (they bypass all security)
# - Rotate keys regularly and when team members leave